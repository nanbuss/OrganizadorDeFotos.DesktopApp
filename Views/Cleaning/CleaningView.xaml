<UserControl x:Class="OrganizadorDeFotos.DesktopApp.Views.Cleaning.CleaningView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F0F0F0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="12">
            <DockPanel>
                <TextBlock Text="Limpieza Inteligente" FontSize="14" FontWeight="Bold" DockPanel.Dock="Left" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button x:Name="AnalyzeButton" Content="Analizar" Click="AnalyzeButton_Click" Padding="10" Background="#0078D4" Foreground="White" FontSize="12" Margin="0,0,8,0"/>
                    <Button x:Name="CleanSelectedButton" Content="Limpiar Seleccionados" Click="CleanSelectedButton_Click" Padding="10" Background="#28A745" Foreground="White" FontSize="12" IsEnabled="False"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Background="White" BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="4" Padding="12" Margin="12,12,12,0">
            <TextBlock Text="Analiza capturas de pantalla y fotos extremadamente oscuras o claras para ayudarte a limpiar archivos no deseados rápidamente." TextWrapping="Wrap" Foreground="#555"/>
        </Border>

        <ScrollViewer Grid.Row="2" Margin="12,12,12,12">
            <ItemsControl x:Name="CandidatesItemsControl" ItemsSource="{Binding TrashCandidates}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="240" Margin="8" Background="White" BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="6" Padding="8">
                            <StackPanel>
                                <Image Source="{Binding ImageSource}" Height="160" Stretch="UniformToFill" Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding FileName}" FontWeight="Bold" TextTrimming="CharacterEllipsis" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding Reason}" Foreground="#333" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding Confidence, StringFormat='Confianza: {0:P0}'}" Foreground="#666" Margin="0,0,0,8"/>
                                <CheckBox Content="Seleccionar" IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Overlay de carga -->
        <Border Grid.Row="0" Grid.RowSpan="3" Background="#88000000" Visibility="{Binding IsAnalyzing, Converter={StaticResource BoolToVisibilityConverter}}" Panel.ZIndex="999">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="280" Height="18" Margin="0,0,0,12" Foreground="#0078D4"/>
                <TextBlock Text="Analizando fotos para limpieza inteligente..." Foreground="White" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
