<UserControl x:Class="OrganizadorDeFotos.DesktopApp.Views.Preview.PreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barra superior -->
        <Border Grid.Row="0" Background="#F0F0F0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="12">
            <DockPanel>
                <TextBlock Text="Vista Preliminar" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" DockPanel.Dock="Left"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <Button x:Name="LoadGalleryButton" Content="🖼️ Cargar Miniaturas" Click="LoadGallery_Click" Padding="10,6" Background="#0078D4" Foreground="White" FontSize="12" Margin="0,0,8,0"/>
                    <Button x:Name="DiscardButton" Content="🗑️ Descartar Seleccionados" Click="DiscardSelectedFiles_Click" Padding="10,6" Background="#FF6B35" Foreground="White" FontSize="12" IsEnabled="False"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Vista de Galería -->
        <Border Grid.Row="1" Background="White" BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="4" Margin="0,12,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="GalleryItemsControl">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Margin="10"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="160" Margin="8" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="5">
                                <Grid>
                                    <StackPanel>
                                        <Grid Height="120">
                                            <Image Source="{Binding ImageSource}" Stretch="UniformToFill"/>
                                            
                                            <!-- Indicador de Reproducción para Videos -->
                                            <Grid Visibility="{Binding IsVideo, Converter={StaticResource BoolToVisibilityConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Ellipse Width="36" Height="36" Fill="Black" Opacity="0.5"/>
                                                <Path Data="M10,8 L10,28 L26,18 Z" Fill="White" Opacity="0.9" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="3,0,0,0"/>
                                                <Grid.Effect>
                                                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.6"/>
                                                </Grid.Effect>
                                            </Grid>
                                        </Grid>
                                        <TextBlock Text="{Binding FileName}" FontSize="10" TextTrimming="CharacterEllipsis" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                    </StackPanel>
                                    <CheckBox IsChecked="{Binding IsSelected}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="5" Click="GalleryCheckBox_Click">
                                        <CheckBox.LayoutTransform>
                                            <ScaleTransform ScaleX="1.3" ScaleY="1.3"/>
                                        </CheckBox.LayoutTransform>
                                    </CheckBox>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Overlay de carga -->
        <Border Background="#88000000" Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}" Grid.RowSpan="2" Panel.ZIndex="100">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="280" Height="18" Margin="0,0,0,12"/>
                <TextBlock Text="Cargando vista preliminar y procesando miniaturas..." Foreground="White" FontSize="14" HorizontalAlignment="Center" TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
