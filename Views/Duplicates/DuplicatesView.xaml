<UserControl x:Class="OrganizadorDeFotos.DesktopApp.Views.Duplicates.DuplicatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Background="#FAFAFA">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Lista de Grupos de Similitud -->
        <Border Grid.Column="0" Background="White" BorderBrush="#D0D0D0" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#F0F0F0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="12">
                    <DockPanel>
                        <TextBlock Text="Grupos de Similitud" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Button x:Name="AnalyzeButton" Content="Analizar" Click="AnalyzeButton_Click" HorizontalAlignment="Right" Padding="10,4" Background="#0078D4" Foreground="White" FontSize="12" DockPanel.Dock="Right"/>
                    </DockPanel>
                </Border>

                <ListBox Grid.Row="1" x:Name="SimilarityGroupsListBox" SelectionChanged="SimilarityGroupsListBox_SelectionChanged" Padding="8">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" ToolTip="{Binding DisplayName}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- Zona de Previsualización -->
        <Grid Grid.Column="1" Background="#FAFAFA">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#F0F0F0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="12">
                <TextBlock Text="Previsualización de Grupo" FontSize="14" FontWeight="Bold"/>
            </Border>

            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <Grid>
                    <!-- Mensaje cuando no hay duplicados -->
                    <TextBlock x:Name="NoDuplicatesMessage" Text="No se encontraron fotos duplicadas" 
                               HorizontalAlignment="Center" VerticalAlignment="Center" 
                               Foreground="#999999" FontSize="16" Visibility="{Binding HasNoDuplicates, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    
                    <!-- ItemsControl para mostrar duplicados -->
                    <ItemsControl x:Name="GroupPreviewItemsControl" Margin="15" Visibility="{Binding HasDuplicates, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5" MinWidth="320">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Ubicación (Carpeta) -->
                                <TextBlock Grid.Row="0" Text="{Binding RelativePath}" Foreground="#999999" FontSize="10" Margin="5,0,5,2" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTip="{Binding FilePath}"/>

                                <!-- Panel de Información -->
                                <Border Grid.Row="1" Background="#F0F0F0" BorderBrush="#D0D0D0" BorderThickness="1" Padding="5" Margin="0,0,0,5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox Grid.Column="0" Content="{Binding FileName}" IsChecked="{Binding IsSelected}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" Text="{Binding SizeKB, StringFormat='Tamaño: {0} KB'}" FontSize="10" VerticalAlignment="Center" Margin="10,0"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Dimensions}" FontSize="10" VerticalAlignment="Center" Margin="10,0"/>
                                        <TextBlock Grid.Column="3" Text="{Binding QualityIndicator}" FontSize="10" FontWeight="Bold" Foreground="Green" VerticalAlignment="Center" Margin="10,0"/>
                                    </Grid>
                                </Border>

                                <!-- Imagen con lupa y badge -->
                                <Border Grid.Row="2" CornerRadius="10" BorderThickness="1" Background="White">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="#D0D0D0"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsHighestQuality}" Value="True">
                                                    <Setter Property="BorderBrush" Value="#D19B20"/>
                                                    <Setter Property="BorderThickness" Value="2"/>
                                                    <Setter Property="Background" Value="#FFFDF2CC"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid ClipToBounds="True">
                                        <Image x:Name="PreviewImage" Source="{Binding ImageSource}" Stretch="Uniform" SnapsToDevicePixels="True"
                                               MouseEnter="PreviewImage_MouseEnter"
                                               MouseLeave="PreviewImage_MouseLeave"
                                               MouseMove="PreviewImage_MouseMove"/>

                                        <Canvas IsHitTestVisible="False">
                                            <Ellipse x:Name="ZoomLens" Width="130" Height="130" Stroke="White" StrokeThickness="2" Visibility="Collapsed" Panel.ZIndex="2" RenderTransformOrigin="0.5,0.5">
                                                <Ellipse.Fill>
                                                    <VisualBrush x:Name="ZoomBrush" Visual="{Binding ElementName=PreviewImage}" ViewboxUnits="RelativeToBoundingBox" Stretch="UniformToFill"/>
                                                </Ellipse.Fill>
                                            </Ellipse>
                                        </Canvas>

                                        <TextBlock Text="RECOMENDADA" Visibility="{Binding IsHighestQuality, Converter={StaticResource BoolToVisibilityConverter}}"
                                                   Background="#D19B20" Foreground="White" FontWeight="Bold" FontSize="10" Padding="6,3" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8" Panel.ZIndex="3"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </ScrollViewer>

            <!-- Botón Procesar Grupo + Configuración -->
            <Border Grid.Row="2" Background="#F0F0F0" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="ProcessAllButton" Content="✅ Procesar Todo" Click="ProcessAll_Click" Padding="10" Background="#0078D4" Foreground="White" FontSize="12" Margin="0,0,8,0" IsEnabled="{Binding HasDuplicates}"/>
                    <Button x:Name="SettingsButton" Content="⚙️ Configuración" Click="SettingsButton_Click" Padding="10" Background="#6C757D" Foreground="White" FontSize="12" Margin="0,0,8,0"/>
                    <Button x:Name="ProcessGroupButton" Content="Procesar Grupo" Click="ProcessGroupButton_Click" Padding="10" Background="#28A745" Foreground="White" FontSize="12" IsEnabled="False"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Overlay de carga -->
        <Border Background="#88000000" Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}" Grid.ColumnSpan="2">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="280" Height="18" Margin="0,0,0,12"/>
                <TextBlock Text="Analizando similitudes y calculando hashes..." Foreground="White" FontSize="14" HorizontalAlignment="Center" TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>